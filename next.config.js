/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/JamesLeePM',
  assetPrefix: '/JamesLeePM/',
};

export default nextConfig;
